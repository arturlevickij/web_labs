import React from "react";
import { LoadingStyled, LoadingContainer } from "./Loading.styled";

const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingStyled />
    </LoadingContainer>
  );
};

export default Loading;
